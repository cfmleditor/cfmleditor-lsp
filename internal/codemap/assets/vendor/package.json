{
  "name": "cfmleditor-lsp-viewer-vendor",
  "private": true,
  "description": "Pins the JavaScript the generated code-map viewer embeds. Nothing here is imported by Go code at build time; make update-d3 copies the built bundle into assets/vendor, which is committed so the Go build stays offline and dependency-free.",
  "dependencies": {
    "d3": "7.9.0"
  }
}
